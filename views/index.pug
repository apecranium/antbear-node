extends layout.pug
block breadcrumb
  li.is-active
    a.has-text-white(href='#') index
block content
  section.section
    .container
      h1.title Hello!
      p Welcome to my app! Click a link below, or explore the 
        a(href='/api/entities') API.
  section.section
    .container
      .tile.is-ancestor
        .tile.is-parent
          article.tile.is-child.box
            p.title
              a(href='/entities') Entities
        .tile.is-parent
          article.tile.is-child.box
            p.title
              a(href='/users') Users
